#include<iostream>
#include<string.h>
using namespace std;
class string1
{
   private:
      char name[100];
   public:
      void get()
      {
         cout<<"enter the string"<<endl;
         cin>>name;
      }
      void display()
      {
         cout<<name<<endl;
      }
      string1 operator +(string1 o1)
      {
         string1 res;
         strcpy(res.name,name);
         strcat(res.name,o1.name);
         return res;
      }
};
int main()
{
   string1 s1,s2,s3;
   s1.get();
   s2.get();
   s3=s1+s2;
   s1.display();
   s2.display();
   s3.display();
}
